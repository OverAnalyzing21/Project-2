<section class="w-100">
    <div class="container">
    <div class="post-list row">
{{#each reviews as |review|}}
{{!-- {{log movie}} --}}
      {{> user-card review}}
    {{/each}}
        </div>
    </div>
</section>